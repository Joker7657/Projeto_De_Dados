CREATE TABLE staging_sales (
    sale_id INT,
    customer_name TEXT,
    product_name TEXT,
    sale_date DATE,
    quantity INT,
    total_value NUMERIC(10,2),
    status TEXT,
    state CHAR(2)
);
